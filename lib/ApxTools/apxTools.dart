import 'package:flutter/material.dart';

class apxTools extends StatelessWidget {
  const apxTools({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Coming Soon!',
          style: TextStyle(
            fontSize: 24,
            fontWeight: FontWeight.bold,
            color: Colors.blue, // You can change this color as needed
          ),
        ),
      ),
    );
  }
}
